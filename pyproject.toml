[project]
name = "pulumi-aws-eks-hello-world-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pulumi>=3.197.0",
    "pulumi-aws>=7.7.0",
    "flask>=3.0.0",
]
